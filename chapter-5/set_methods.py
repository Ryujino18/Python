s = {1,2,3,3,5,6, "Ryujino", "Ryujino"} # in set we can also add the names, but cant repeat them 
# print(s , type(s))
# s.add("hi")
print(s) 

#sets are unordered, unindexed
# there is no way to change items in sets, cannot contain duplicate values

print(len(s))
s.remove(1)
print(s)
s.pop() # Randomy remove any element from the sets
print(s)