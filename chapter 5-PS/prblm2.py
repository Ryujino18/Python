s = set()
for i in range (7):
    n = input("Enter the number:")
    s.add(int(n))
print(s)

# == checks if two operands are of the same type, if they are not, it triesto convert one of them into another